% To call the function you have to be in the same directory as function file is.

% Functions should have the same name as file is named.
sq = squareNumber(5);

% It is possible to return several values from the function.
[sq, cube] = squareAndCubeNumber(2);
